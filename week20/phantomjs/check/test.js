var page = require('webpage').create();
page.open('https://baidu.com', function(status) {
  if(status === "success") {
    if(status === "success") {
        page.render('example.png');
    }
  }
  phantom.exit();
});